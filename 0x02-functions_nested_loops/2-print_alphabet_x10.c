#include"main.h"

/**
 * print_alphabet -function to print
 *                 the alphabet a - z
*/

void print_alphabet(void)
{
	int alpha;

	for (alpha = 'a'; alpha <= 'z'; ++alpha)
		_putchar(ch);
	_putchar('\n');
}
